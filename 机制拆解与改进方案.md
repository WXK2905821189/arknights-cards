# 罗德岛棋局 · 货币战争 — 机制拆解与改进方案（Mechanic Biopsy）

> 拆解对象：`game.html` + `game.js`（2090 行）+ `data.js`（136 干员）+ 羁绊配置（`BONDS/SIGNATURE/SPECIAL`）。
> 方法：先读透实机代码，再对照《特殊羁绊机制设计.md》做机制活检（Biopsies）。结论优先，每条发现带定位与验证路径。
> 所有未经实机 playtest 的数值标 **[PLACEHOLDER · 假设与验证路径]**。

---

## ✅ 落地状态（2026-08-11）
> P0（策略增益接入 / 10 格溢出）已于前一轮闭环；**本轮 P1 + P2 全部落地**：P1-1 羁绊软上限、P1-2 战败扣血平滑+落后补给、P1-3 激活 10 个干员的死技能形态（shield/stun/buff/dot）、P2-1 分支地图、P2-2 Meta 战利品库、P2-3 单势力独行被动、P2-4 战败复盘面板。Node 冒烟 18/18 全绿，临时脚本已删。下方 F3–F9 的 `[PLACEHOLDER]` 数值（软上限 6.5 / 扣血 22% / Meta 币额）仍待实机 playtest + 镜像对局标定。

---

## 〇、一句话结论

**好玩的内核成立**：这是一个 TFT 内核扎实的自走棋——有限经济 + 有限人口下，凑三层羁绊、养三星与签名核心，看阵容在对撞中胜过敌人。循环、经济、战斗引擎都搭得完整。

**但有三类硬伤拖后腿**：
1. 🔴 **建了却没生效**（策略节点的"全体+%攻击/生命/攻速/法强" 7 张卡完全不进战斗；5 个技能形态分支是死代码）。
2. 🔴 **会崩的边界**（人口上限可超过 9 格，第 10 个单位隐形部署）。
3. 🟠 **数值超线性膨胀 + 线性地图**：羁绊乘法叠乘无软上限，对撞变成"谁数值大谁赢"；27 节点一条直线，中期无路径选择，agency 弱。

---

## 一、核心循环拆解（Core Loop）

| 层级 | 周期 | 玩家此刻的核心决策 | 张力来源 |
|---|---|---|---|
| **Moment-to-moment** | 0–30s / 单节点 | 买谁 / 拖谁上场 / 刷新 / 买经验 | 钱不够、格子不够、羁绊差 1 人 |
| **Session loop** | 5–15min / 一局 | 走哪条羁绊路线、何时拉人口、何时梭哈三星 | 经济效率 vs 阵容强度 |
| **Long-term** | 一局结束 | 难度选择 + 开局环境押注；通关晋升计数 | 重开动机（目前仅装饰） |

**节点地图（固定线性，27 节点 / 3 阶段）**（`buildNodes`）：
- 阶段一：补给×2 开头 → 普通战×3 → 策略 → 遭遇 → 补给 → 普通战
- 阶段二 / 三：普通战为主，各含 1 策略 + 1 遭遇；阶段三末尾为 **BOSS**
- 节点类型：`battle / elite / encounter / boss / strategy / reward`，**顺序写死、无分支**。

---

## 二、经济系统（Sources & Sinks）

每回合收入（`enterShopRound`）：
`收入 = min(回合+2, 7) [基础] + min(5+interestMax, ⌊金/10⌋) [利息] + 连胜/连败奖励(1/2/3) + 策略 goldPerRound(2/4)`

| 类别 | 条目 | 公式 / 数值 | 状态 |
|---|---|---|---|
| **Source** | 基础收入 | `min(round+2, 7)`，第 5 回合封顶 7 | ✅ |
| | 利息 | `min(5+interestMax, ⌊gold/10⌋)`，环境"复利引擎"→上限+5 | ✅ 攒钱有回报 |
| | 连胜/连败 | ≥2→+1，≥4→+2，≥6→+3（**胜负都给**，橡皮筋友好） | ✅ |
| | 策略/环境 | 理财+2/厚赏+4/资本注入+12（开局一次性） | ✅ |
| | 补给节点 | `rnd(8,14) × rewardMult`，60% 概率再送 1–2 干员 | ✅ |
| | 胜利奖励 | 精英 +6×rm；遭遇 `rnd(10,16)×rm×encMult` | ✅ |
| | 售出返还 | `cost × (1/3/9 by 星) × (1+售出%)` | ✅ |
| **Sink** | 购买单位 | cost 1–5（环境"罗德岛特供"-1） | ✅ |
| | 刷新商店 | 2 💰 | ✅ |
| | 买经验 | 4 💰 → +4 exp | ✅ |
| | **高阶消费** | **无**（满编+满星后金币无处可花） | ⚠️ 轻度通胀死角 |

**通胀风险评估**：中期有利息/连胜吸收金币，平衡尚可。但**后期唯一 sink 是刷新+买经验**，满人口满星后金币溢出无去处——属于"温和死角"，不构成爆炸，但削弱了后期决策。建议加一个**可消费的进阶 sink**（见改进 P2-2）。

---

## 三、羁绊系统（三轴 + 签名 + 特殊）

| 轴 | 阈值 | 效果风格 | 备注 |
|---|---|---|---|
| **职业**（8 类） | 2 / 4 / 6 人 | 主题化（先锋技力流、近卫攻速、重装壁垒…） | 主副属性分两档抬升 |
| **阵营**（34 取值，含 `__default__` 兜底） | 2 / 3 / 5 人 | 势力主题化（炎=血防、拉特兰=攻暴…） | 池<4 的势力自动套兜底通用攻血 |
| **签名**（15 个 cost=5） | 上场即生效 | 单人被动（属性 attr + 行为 kw） | 无需凑人数 |
| **特殊**（23 阵营 capstone） | 按其池最高档解锁 | 行为关键字（灼烧/破甲/养狼/处决…） | light 版，召唤无独立经验条 |

**叠乘数学（危险点）**：单卡最终攻击 ≈
`基础atk × STAR_MULT(最高3.2) × 职业mult(最高1.32) × 阵营mult(最高1.26) × 签名attr × 特殊attr`
——四~五项**乘法串联**，一个满配三星签名核心可达基础值的 **5–8 倍**，且没有任何软上限。设计文档自身的"（原 100% 超模）""二次削弱"注释，证明这是**靠事后 reactive 砍数值在压制**，而非结构性约束。

---

## 四、战斗引擎（Combat Sim）

- **战场**：8×4 网格，我方左 4 列 / 敌方右 4 列；`DT=0.1`，`MAX_T=60` 秒。
- **行动**：攻速决定攻击间隔 `ATK = 100/(spd×减速×施法加速)`；够不到则按切比雪夫距离移动。
- **伤害公式**（`dealDamage`）：法强加算 → 防御减免（pierce 无视部分防）→ 真伤追加 → 暴击+暴伤 → 处决 → 受击减伤 → 龙门协防 → 护盾吸收；附带破甲/减速/吸血/反伤/灼烧。
- **技能**（攒满技力释放）：burst/aoe/execute/heal/debuff 实际有干员使用；**shield/stun/buff/dot/summon 五个分支在数据里无干员触发**（summon 分支实际只给护盾）。
- **召唤**：叙拉古达阶二/三开战前生成 1/2 只狼、令上场生成 1 只岁兽，固定数值、无独立经验条（设计文档记为 TODO）。

---

## 五、机制活检发现（Findings）

### 🔴 P0 — 阻断 / 信任级（必须修）

**F1 · 策略节点战斗增益完全未接入战斗（7 张卡是死的）**
- 定位：`aggregateStrategies()` 只在 `enterShopRound`(L1224) 与 `sellUnit`(L1401) 被调用，且只读取 `goldPerRound/expPerRound/freeReroll/boardCapBonus/sellValuePct`。`allAtkPct / allHpPct / allAspdPct / allMagicPct` **算出来后从未被消费**。
- 受影响卡：锋锐(+8%攻)、坚壁(+8%血)、急袭(+8%速)、战意(+15%攻/+10%血)、强军(+12%攻/+8%血)、天启(+20%攻/+15%血/+12%速)、战术核心(+10%法强)。
- 玩家影响：**明确写着"全体+%，点了等于没点"**，直接破坏策略系统的信任。这是最严重的设计-实现脱节。

**F2 · 部署人口可超过 9 格 → 第 10 个单位隐形部署**
- 定位：`boardCap() = G.level + G.boardBonus`；环境"前线扩编"给 `boardCapBonus:+1`，满级 9 → 上限 10。但 `renderBoard` 只循环 `i<9` 渲染 9 格（`game.js` L942）。`firstFreeSlot` 却按 `boardCap()` 最多返回槽位 9。
- 后果：触发"前线扩编"后，第 10 个单位能进 `G.board`、进战斗阵列，但**板上无格子可点、画面不可见**，且拖拽/售出逻辑会混乱。潜伏 bug。

### 🟠 P1 — 平衡 / 公平

**F3 · 羁绊乘法叠乘无软上限**
- 见第三节数学。后果：阵容强度由"乘法层数"决定而非博弈，对撞变成数值比大小，counterplay 被掏空。

**F4 · 战败扣血 cliff（满血即死风险）**
- 定位：`dmg = (敌方存活×4 + 3) × hpLossMult`（L1630）。`hpLossMult` 在梦魇=2.0，敌方存活可达 9 → 单场扣 **(36+3)×2 = 78**。满血 100 几乎一回合崩。
- 对比：胜利奖励是金币/经验，失败惩罚却是近乎秒杀的扣血，曲线不对称过陡。

**F5 · 5 个技能形态是死代码，战斗手感同质化**
- 实际技能分布：burst 82 / aoe 39 / execute 6 / heal 8 / debuff 1。shield/stun/buff/dot 全空。
- 后果：132/136 干员的主动技都是"打伤害或奶"，**控制/护盾/增益维度缺失**，战斗读起来是一锅伤害粥，缺乏 TFT 那种"这台专破甲、那台专控场"的角色差异。

### 🟡 P2 — 深度 / 留存

**F6 · 线性 27 节点无分支**
- 节点类型顺序写死。玩家唯一的"选择"在：开局难度、开局环境、策略节点三选一、遭遇节点三档。
- 中期（大量普通战）**没有任何路径抉择**，agency 弱，重玩差异主要靠随机商店而非玩家决策。

**F7 · 晋升计数纯装饰，无 meta 进度**
- 通关只 `localStorage` 记一个 `promote` 数字，无任何跨局解锁。长期留存钩子几乎为零。

**F8 · 11 个单干员势力无任何被动**
- 格拉斯哥帮/黑钢/萨米等池=1 阵营无法成羁绊，只能吃职业+签名（设计文档 TODO，未实现）。这些单位"没灵魂"。

**F9 · 战败反馈只有一行文本，无复盘通道**
- "损失 X 生命"——玩家不知道**为什么输**（是输出不够、前排太脆、还是羁绊没凑齐）。反馈通道缺失，违反"先查反馈再怀疑机制"。

---

## 六、改进方案（按优先级，每条含 purpose / 输入 / 输出 / 边界 / 失败态 + rationale + 数值）

### P0-1 · 把策略战斗增益真正接入战斗
- **Purpose**：恢复策略系统信任，让"全体+%"名副其实。
- **输入**：`aggregateStrategies()` 已算好的 `allAtkPct/allHpPct/allAspdPct/allMagicPct`。
- **改动**：给 `applyBonds(units, side, battleBuff)` 增参数；在 `makeCombatUnit` 末尾对 `atk/maxHp(连 hp)/spd/magicAmp` 再乘 `(1+battleBuff.x)`（仅 ally 侧吃玩家的策略；敌方不享受）。`onFight` 里 `const se = aggregateStrategies();` 传入。
- **边界**：magicAmp 只影响法伤；spd 改变需同步重置 `next`。
- **数值**：沿用现有卡面数值（0.08/0.12/0.15/0.20），无需新增。
- **失败态**：接入后镜像对局显示 ally 输出显著上升；任一策略卡在 `simulateBattleGrid` 前后 DPS 有可测差异。

### P0-2 · 修复 10 格溢出（二选一）
- **推荐（真修）**：把部署区从 3×3=9 扩成 **4×3=12**（同时容纳"前线扩编+1"及未来 +2），更新 `slotToXY` 索引与 CSS 网格。
- **快速补丁**：`boardCap = Math.min(9, G.level + boardBonus)`，并把环境"前线扩编"改为"开局直接 Lv.+1"或"+1 商店出现高费概率"。
- **rationale**：9 格硬约束与"+1 人口"环境根本冲突，要么扩板要么改环境语义，不能并存。
- **数值**：[PLACEHOLDER · 扩板后 ODDS/人口曲线需重画，假设平均单局 12–15 节点，验证后再定]。

### P1-1 · 羁绊乘法软上限
- **Purpose**：保留 build 表达，压制一击秒杀。
- **改动**：在 `makeCombatUnit` 收尾对最终 `atk`、`maxHp` 乘数设软上限，例如 `finalAtkMult = min(rawMult, 6)`。
- **数值**：[PLACEHOLDER · 建议 5.5–6.5×，需用镜像对局 Monte Carlo 标定：取"胜率 50% 的交战双方乘数差"作为天花板]。
- **失败态**：满配三星签名核心仍强，但不再能在 1 个技能循环内清空对方满血前排。

### P1-2 · 战败扣血平滑 + 落后补给
- **改动**：`dmg = min( maxHp×0.22 , (敌方存活×系数+基数)×hpLossMult )`，BOSS 战单独上限更高 [PLACEHOLDER]。同时连败额外给 1 次免费刷新，强化现有橡皮筋。
- **rationale**：把"-78 即死"改成"可控的 22% 上限"，失败仍有翻盘空间，符合"共情是起点"。

### P1-3 · 激活死技能形态 / 重塑战斗角色差异
- **改动**：从现有 136 干员中挑 10–15 个，把技能 `archetype` 改写为 `shield/stun/buff/dot`（代码分支已就绪，零新逻辑）；例如让重装带 `shield`、控制型带 `stun`、辅助带 `buff`。
- **rationale**：战斗维度从"纯伤害"扩到"破甲/控场/护盾/增益"，counterplay 与角色辨识度同时回来。
- **数值**：[PLACEHOLDER · shield 量按施法者 atk×mult；stun 时长 1.0–1.5s；需逐职业 playtest]。

### P2-1 · 分支节点地图（roguelite）
- **Purpose**：补齐中期 agency，是长期留存最大杠杆。
- **改动**：每个非强制节点给出 2–3 个候选（如"精英战 / 补给 / 商店事件"三选一），玩家走图。
- **边界**：BOSS 前的补给、阶段结构保留。
- **数值**：[PLACEHOLDER · 分支权重与出现池需设计，假设每局 8–10 个真正的抉择点]。

### P2-2 · Meta 进度货币
- **Purpose**：给"重开"一个理由。
- **改动**：通关 BOSS 给跨局货币，解锁：开局可选环境+1、起始等级+1、棋盘皮肤、额外免费刷新等。
- **rationale**：把纯装饰的 `promote` 计数升级成有意义的成长。

### P2-3 · DEPLOY_PASSIVE（单势力被动）
- **改动**：新增 `DEPLOY_PASSIVE` map，11 个池=1 阵营干员上场即给轻量风味被动（如推进之王"部署后渐升攻防"已部分在签名里，可外推）。
- **rationale**：让每个单位都"有东西"，消除 F8 的空洞感。

### P2-4 · 战败复盘面板
- **改动**：战斗结束新增一行"复盘"：双方阵容羁绊数、平均星级、你的输出/承伤、关键死因（前排先倒 / 输出不足 / 被处决）。
- **rationale**：补上缺失的反馈通道（F9），玩家才知道下一步该补什么。

---

## 七、Playtest 失败信号（先定义"坏掉长什么样"）

| 信号 | A（致命） | B（偏差） | C（可接受） |
|---|---|---|---|
| 策略增益接入(P0-1) | 任一战斗增益卡 DPS 无变化 | 仅某一属性生效 | 全属性生效、胜率曲线平滑上升 |
| 10 格溢出(P0-2) | 仍出现不可见单位 | 仅边界槽位错位 | 满编 9/扩板 12 均正常 |
| 羁绊软上限(P1-1) | 满配仍 1 循环清空前排 | 上限过高仍秒杀 | 强但需 2–3 循环 |
| 战败扣血(P1-2) | 梦魇仍接近即死 | 上限仍 >30% | 单场 ≤22% 且可翻盘 |

---

## 八、落地顺序与风险

1. **P0-1 + P0-2**：纯接线/边界修复，低风险，先做（预计 1 个 session）。修复后策略系统和部署才"是真的"。
2. **P1-1 + P1-2**：数值手术，需镜像对局 + Monte Carlo 标定，标注 PLACEHOLDER。
3. **P1-3**：改数据 `archetype`，不动引擎，中低风险。
4. **P2-1 / P2-2 / P2-3 / P2-4**：结构扩展，工作量最大，建议单独排期。

> 红线确认：以上数值除已落地的卡面值外，一律 **[PLACEHOLDER]**，必须经实机 playtest 与镜像对局标定后再定稿；不在假设上锁死数值。
